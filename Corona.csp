	.EQU @, $000
	.EQU KEYBOARD_STATUS, $000
	.EQU KEYBOARD_DATA, $001
WORD:	.CHAR'get pwned', charlen

	LDX# 0
POLL_KEYB:	INB KEYBOARD_STATUS
	JGE POLL_KEYB
	INB KEYBOARD_DATA
	STA+ $D00
	CMX# 9
	JGE CHECK
	NOP
	ADX# 1
	JMP POLL_KEYB

CHECK:

END:	HLT